# Generated by `skillspec import-skill` and semantically reviewed.
# Review before proof, install, or release. Do not delete missing dependencies to make QA pass.
schema_version = 1
generated_by = "skillspec import-skill"
review_required = false
dependency_count = 2
quarantined_count = 0

[[dependency]]
id = "executor_desktop_mcp"
ecosystem = "service"
authority = "reviewed_runtime_dependency"
source = "confluence-pages/SKILL.md and reference/operations.md"
reason = "Confluence reads and approved writes run through executor-desktop MCP's atlassian_mcp integration."
local_status = "deferred"
required_for = ["all_routes"]
degraded_without = true
risk = "If executor-desktop or the Atlassian integration is unavailable, stop and report the missing access surface instead of guessing another write path."
install = "Configure the executor-desktop MCP with the authenticated atlassian_mcp integration."

[[dependency]]
id = "executor_cli"
ecosystem = "cli"
authority = "reviewed_runtime_dependency"
source = "confluence-pages/SKILL.md attachment non-negotiables and reference/operations.md v2 upload branch"
reason = "Attachment preparation, local byte upload, OAuth repair, and v2 tool discovery use the supported Executor CLI."
local_status = "available"
required_for = ["personal_space_media_update"]
degraded_without = true
risk = "Without the Executor CLI, do not claim local attachment upload is impossible; report that the v2 upload path could not be exercised."
install = "Install Executor from https://github.com/UsefulSoftwareCo/executor and authenticate the local server."
