# Generated by `skillspec import-skill`.
# Review before proof, install, or release. Do not delete missing dependencies to make QA pass.
# Dependency extraction is evidence-based: executable commands become cli dependencies, declared services become service/api dependencies, package-manager evidence becomes package dependencies, and code examples stay reference-only unless promoted during review.
schema_version = 1
generated_by = "skillspec import-skill"
review_required = false
dependency_count = 3
quarantined_count = 0

[[dependency]]
id = "firecrawl"
ecosystem = "cli"
authority = "command_evidence"
source = "skillspec import-skill"
reason = "Inferred CLI dependency from imported skill material: firecrawl"
local_status = "unknown"
required_for = ["skill_runtime"]
degraded_without = true
risk = "Reviewed as required for Firecrawl routes; if missing or unauthenticated, stop before paid calls and ask before fallback."
install = ""

[[dependency]]
id = "parallel-cli"
ecosystem = "cli"
authority = "command_evidence"
source = "skillspec import-skill"
reason = "Inferred CLI dependency from imported skill material: parallel-cli"
local_status = "unknown"
required_for = ["skill_runtime"]
degraded_without = true
risk = "Reviewed as required for Parallel routes; if missing or unauthenticated, stop before paid calls and ask before fallback."
install = ""

[[dependency]]
id = "executor_mcp"
ecosystem = "service"
authority = "reviewed_runtime_dependency"
source = "Kevin-provided Executor MCP URL"
reason = "Exa, DeepWiki, Perplexity, TinyFish, and hosted Parallel/Firecrawl are accessed exclusively through Kevin's Executor MCP endpoint at https://executor.sh/kevin-chen-s-organization/mcp."
local_status = "deferred"
required_for = ["exa_executor", "deepwiki_executor", "perplexity_executor", "tinyfish_fetch", "tinyfish_executor", "hosted_parallel_or_firecrawl_executor"]
degraded_without = true
risk = "Requires live MCP catalog discovery before any hosted provider use; connection path segments change on reauthorization. If a provider is not discoverable through this MCP endpoint, report it unavailable instead of guessing tool names or substituting local Executor sources."
install = "Configure the harness MCP connection to https://executor.sh/kevin-chen-s-organization/mcp with the appropriate bearer credential."
